# Generated by devsec — secure CI/CD pipeline for Bitbucket Pipelines.
# Steps are real and runnable. Set repository variables SONAR_HOST_URL,
# SONAR_TOKEN (and SNYK_TOKEN) in Bitbucket. Scanner reports are saved as
# step artifacts. Bitbucket clones the repo automatically.
{{ bitbucketRender . }}
