// Generated by devsec — secure CI/CD pipeline for Jenkins (declarative).
// Review agent and pin tool versions before production use.
pipeline {
    agent any

    options {
        timestamps()
    }

    stages {
{{- range .Stages }}
        stage('{{ .Name }}') {
            steps {
{{- if .Tools }}
{{- range .Tools }}
                // Recommended tool: {{ . }}
                sh 'echo "TODO: invoke {{ . }} here (devsec orchestrates, it does not bundle scanners)"'
{{- end }}
{{- else }}
                sh 'echo "TODO: implement {{ .Name }}"'
{{- end }}
            }
        }
{{- end }}
    }
}
