# Generated by devsec — secure CI/CD pipeline for GitLab CI.
# Steps are real and runnable. Set CI/CD variables SONAR_HOST_URL, SONAR_TOKEN,
# (and SNYK_TOKEN) in your GitLab project. Scanner jobs use allow_failure: true
# initially — flip to false to hard-gate once the baseline is clean.
{{ gitlabRender . }}