# Generated by devsec — secure CI/CD pipeline for GitLab CI.
# Review images and pin versions before production use.
stages:
{{- range .Stages }}
  - {{ .ID }}
{{- end }}

{{ range .Stages }}
{{ .ID }}:
  stage: {{ .ID }}
  script:
{{- if .Tools }}
{{- range .Tools }}
    - echo "TODO: invoke {{ . }} here (devsec orchestrates, it does not bundle scanners)"
{{- end }}
{{- else }}
    - echo "TODO: implement {{ .Name }}"
{{- end }}
{{ end }}
