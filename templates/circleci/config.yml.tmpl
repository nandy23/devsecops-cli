# Generated by devsec — secure CI/CD pipeline for CircleCI.
# Steps are real and runnable. Set project environment variables SONAR_HOST_URL,
# SONAR_TOKEN (and SNYK_TOKEN) in CircleCI. Jobs are chained in the workflow so
# security stages gate later ones; scanner reports are stored as artifacts.
{{ circleciRender . }}
