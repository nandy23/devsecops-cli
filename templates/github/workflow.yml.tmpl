# Generated by devsec — secure CI/CD pipeline for GitHub Actions.
# Steps are real and runnable. Review, pin action versions to commit SHAs, and
# set required secrets (e.g. SONAR_TOKEN, SONAR_HOST_URL, SNYK_TOKEN) before use.
name: {{ .Name }}

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  security-events: write

jobs:
{{- range .Stages }}
{{ githubJob . $.Lang }}
{{- end }}
